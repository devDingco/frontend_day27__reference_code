# frontend_day27__reference_code

### 공통

- [ ]  완성된 `day26` 폴더를 활용하여 `day27` 을 완성해 주세요.

---

### 게시글 목록 (공통)

- [ ]  게시글 목록 컴포넌트 (`boards-list`)를 **게시글 목록**과 **배너** 두 개의 하위 컴포넌트로 분리해요.
    - 목록: `src/components/boards-list/list`
    - 배너: `src/components/boards-list/banner`
- [ ]  기존 `boards-list` 컴포넌트의 모든 관련 파일을 새로 생성된 `src/components/boards-list/list` 폴더로 옮겨주세요.
    - **이동할 파일**:
        - `hook.ts`
        - `index.tsx`
        - `queries.ts`
        - `styles.module.css`
        - `types.ts`

### 게시글 목록 (조립)

- [ ]  위에서 생성한 **배너**와 **게시글 목록** 컴포넌트를 불러와 완성된 게시글 목록 페이지를 구성해 주세요.
    - [ ]  **경로:** `src/app/boards/page.tsx`

---

### 게시글 목록 (배너)

- [ ]  배너 UI를 구현해 주세요.
    - [ ]  경로: `src/components/boards-list/banner/index.tsx`
    - [ ]  여러 이미지가 슬라이드 되도록 캐러셀(carousel) 라이브러리를 사용해 주세요. **Swiper** 또는 **react-slick**을 추천해요.

---

### 게시글 등록

- [ ]  경로: `src/components/boards-write/index.tsx`
- [ ]  우편번호 및 주소 등록 기능을 추가해 주세요.
    - [ ]  해당 기능 구현을 위해 `react-daum-postcode` 라이브러리를 사용해 주세요.
    - [ ]  `[우편번호 검색]` 버튼을 누르면 모달로 우편 번호 검색 화면이 나오도록 구현해 주세요.
    - [ ]  우편 번호의 검색이 끝나면, ‘`우편 번호`’와 ‘`기본 주소`’ 2개는 각각의 인풋에 `value` 값으로 추가해 주세요.
    - [ ]  상세 주소는 사용자가 직접 입력하도록 구현해 주세요.
    - [ ]  게시글 등록 API `createBoard` 를 통해 우편 번호, 기본 주소, 상세 주소를 함께 전송하도록 추가해 주세요.
    - [ ]  ‘`우편 번호`’, ‘`기본 주소`’, ‘`상세 주소`’는 필수입력이 아니므로, 입력되지 않아도 게시글은 등록되어야 합니다.
- [ ]  유튜브 주소 등록 기능을 추가해 주세요.

### 게시글 수정

- [ ]  **경로:** `src/app/boards/[boardId]/edit/page.tsx` or `관련 컴포넌트 경로`
- [ ]  우편번호 및 주소 수정 기능을 추가해 주세요.
    - [ ]  게시글 등록 시 입력 되었던 값을 `fetchBoard` API를 사용하여 불러와 주세요.
    - [ ]  우편 번호, 기본 주소, 상세 주소가 존재하는 경우 해당 인풋에 초기 값으로 데이터를 바인딩 해주세요.
    - [ ]  우편 번호 검색을 다시 클릭하여 변경한 경우 기존에 입력된 값이 변경돼야해요.
    - [ ]  `[수정하기]` 버튼 클릭 시 `updateBoard` API를 통해 우편 번호, 기본 주소, 상세 주소가 함께 전송되어 수정 사항에 반영돼야 해요.
- [ ]  유튜브 주소 수정 기능을 추가해 주세요.

---

### 게시글 상세

- [ ]  게시글 상세 페이지의 **좋아요, 싫어요 아이콘**을 수정해 주세요.
    - [ ]  **경로:** `src/app/boards/[boardId]/page.tsx` or `관련 컴포넌트 경로`
    - [ ]  기존 이미지로 구현한 아이콘이 아닌 UI 라이브러리의 아이콘 컴포넌트를 활용해 주세요.
        - 라이브러리 제한은 없으며, MUI 또는 Ant Design 사용을 추천해요.
        - 개인의 취향에 맞는 아이콘으로 자유롭게 변경해도 좋아요.
- [ ]  우편번호 및 주소 조회 기능을 추가해 주세요.
    - [ ]  우편번호, 기본주소, 상세 주소가 조회될 수 있도록 구현해 주세요.
        - [ ]  주소 아이콘에 마우스를 올리면, 라이브러리를 사용하여 주소를 `tooltip` 으로 보여주세요.
            - 라이브러리 제한은 없어요.
        - [ ]  우편 번호, 기본 주소, 상세 주소 중 사용자에게 필요한 정보가 무엇인지 고민해보고, 보여주고 싶은 내용을 선택하여 보여주세요.
- [ ]  게시글 등록 시 작성한 유튜브 주소의 영상을 볼 수 있도록 구현해 주세요.
    - 라이브러리 선택에는 제한이 없으며, 스스로 찾아서 적용해 보세요.
    - 유튜브 영상은 게시글 상세 페이지의 컨텐츠 내용 아랫 부분에 위치시켜 줍니다.
        - 피그마 참고

### 게시글 상세 (댓글 등록)

- [ ]  댓글 등록 시 별점을 줄 수 있도록 기능을 추가해 주세요.
    - [ ]  경로: `src/components/boards-detail/comment-write/index.tsx`
    - 라이브러리 제한은 없으며, MUI 또는 Ant Design 사용을 추천해요.
- [ ]  댓글 조회 시 등록된 별점을 볼 수 있도록 구현해 주세요.
    - [ ]  경로: `src/components/boards-detail/comment-list/index.tsx`
    - [ ]  댓글 등록 시 사용한 라이브러리를 그대로 사용해요.
